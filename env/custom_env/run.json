{
  "pre": [{
    "env": "CUSTOM_ENV",
    "description": "Set the CUSTOM_ENV environment variable",
    "default": "this is an example default value"
  }],
  "run": [{
    "method": "log",
    "params": {
      "raw": "{{env.CUSTOM_ENV}}"
    }
  }]
}
