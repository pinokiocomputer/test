{
  "run": [{
    "method": "log",
    "params": {
      "json2": {
        "env": "{{envs}}",
        "env2": "{{kernel.envs}}",
        "env3": "{{kernel.env}}",
        "env4": "{{env}}"
      }
    }
  }]
}
