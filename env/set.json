{
  "run": [{
    "method": "log",
    "params": {
      "json2": "{{env}}"
    }
  }, {
    "method": "env.set",
    "params": {
      "PINOKIO_SHARE_CLOUDFLARE": "true"
    }
  }, {
    "method": "log",
    "params": {
      "json2": "{{env}}"
    }
  }]
}
