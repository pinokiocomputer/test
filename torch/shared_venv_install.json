{
  "run": [{
    "method": "script.run",
    "params": {
      "uri": "https://github.com/cocktailpeanutlabs/torch.git/install.js",
      "params": {
        "venv": "{{path.resolve(cwd, 'env')}}"
      }
    }
  }, {
    "method": "fs.share",
    "params": {
      "venv": "env"
    }
  }]
}
